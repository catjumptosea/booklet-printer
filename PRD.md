# PDF 小册子打印工具 MVP 需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 小册子打印工具（暂定名 Booklet Press，正式名待定） |
| 文档版本 | v0.1.1（MVP） |
| 日期 | 2026-09-03 |
| 状态 | 已按实现校准 |

---

## 1. 背景与目标

现有 PDF 软件打印小册子存在四个核心痛点：

1. 操作繁琐，需要手动分两次打印正面和反面；
2. 拼版会打乱页序，预览不直观，容易打印出错；
3. 页数不是 4 的倍数时需要手动补页，容易弄错；
4. 想在一张 A4 纸上打印半本小册子以节约纸张时，操作更复杂。

**MVP 目标**：跑通"上传 PDF → 自动拼版 → 直观预览 → 导出打印文件"的核心链路，解决痛点 1、2、3；痛点 4（半页拼版）放到二期。

**成功标准**：用户上传一份 PDF 后，无需理解拼版知识，3 分钟内完成预览确认并拿到可直接打印的文件，且打印折叠装订后的页序完全正确。

## 2. 目标用户与场景

目标用户：需要把文档装订成小册子的普通电脑用户，例如活动组织者（手册、节目单）、教师（讲义、作文集）、个人用户（菜谱、剧本、说明书）。

典型场景：用户手里有一份 8~64 页的 PDF，家里或办公室有一台普通打印机（支持或不支持自动双面均可），希望打印折叠后成为一本 A5 大小的骑马订小册子。

## 3. MVP 范围

### 3.1 本期做（In Scope）

1. PDF 上传与本地解析；
2. 页数检测、自动补空白页（补足 4 的倍数）；
3. 标准骑马订拼版（横向 A4 纸张，每面左右各 1 页，成品 A5 竖开本）；
4. 两种预览视图：纸张视图（打印视角）+ 翻页视图（阅读视角）；
5. 导出双面打印单 PDF；
6. 导出手动双面两个 PDF（正面文件 + 反面文件）；
7. 手动双面打印指引（图文步骤）。

### 3.2 本期不做（Out of Scope，列入二期）

1. 半页拼版（一张 A4 纸打印两本小册子后裁开）；
2. 爬移（creep/shingling）补偿——页数较多时内侧页边距外移的修正；
3. Letter 及自定义纸张尺寸；
4. 装订位置参考线、骑马订钉位置标记；
5. 批量处理多个文件；
6. PWA 离线使用；
7. 账号体系、历史记录、云端存储。

## 4. 核心用户流程

```
打开网页
  → 拖拽/选择 PDF 文件
  → 本地解析（页数、页面尺寸）
  → 展示小册子概要（页数、纸张数、是否补空白页）
  → 预览
      ├─ 纸张视图：逐张查看每张纸正反面（打印视角）
      └─ 翻页视图：模拟成品翻页（阅读视角）
  → 选择导出模式
      ├─ 双面打印：导出 1 个 PDF
      └─ 手动双面：导出 2 个 PDF + 打印指引
  → 下载文件，按指引打印
```

全程页面不刷新，无网络上传。

## 5. 功能需求

### F1 文件上传与解析

**交互逻辑**

- 首屏提供拖拽区域 + 点击选择按钮；
- 仅接受 `.pdf` 文件；文件大小上限 100 MB；
- 选择文件后立即在本地解析，显示解析中状态（loading）；
- 解析完成后进入预览页。

**异常边界**

| 情况 | 处理 |
| --- | --- |
| 非 PDF 文件 | 拖拽时提示"仅支持 PDF 文件"，不进入解析 |
| 加密 PDF（有打开密码） | 明确报错："该 PDF 已加密，请先解密后再试" |
| 损坏/无法解析的 PDF | 明确报错："文件无法读取，请确认文件未损坏" |
| 超过 100 MB | 提示文件过大，建议拆分 |
| 超过 300 页 | 提示"页数较多，处理可能缓慢"，允许用户继续（不硬性拒绝） |
| 0 页 PDF | 按损坏文件处理 |

**数据规则**

- 解析获得：总页数 N、每页的宽高与旋转角度；
- 所有处理在浏览器内存中完成，不写入服务器。

### F2 页数检测与空白页补齐

**数据规则**

- 小册子需要页数为 4 的倍数。设有效页数 N，补空白页数 B = (4 - N % 4) % 4，拼版总页数 N' = N + B；
- 纸张数 S = N' / 4（每张纸打印正反两面，共 4 个页面位置）；
- 空白页统一追加在文档**末尾**（MVP 不提供自定义空白页位置）。

**交互逻辑**

- N 是 4 的倍数时，概要区显示"共 N 页 → S 张 A4 纸"；
- N 不是 4 的倍数时，显示醒目提示："原文档 N 页，已自动在末尾补充 B 页空白，共 N' 页 / S 张纸"；
- 空白页在所有预览视图中以带"空白页"角标的样式显示，与真实内容页明确区分。

### F3 拼版规则（核心算法）

**规则定义**（骑马订标准拼版，横向 A4 纸张，每面左右各排 1 页）

设拼版总页数 N'（4 的倍数），第 i 张纸（i 从 1 开始）：

- **正面**：左半 = 第 (N' - 2i + 2) 页，右半 = 第 (2i - 1) 页；
- **反面**：左半 = 第 (2i) 页，右半 = 第 (N' - 2i + 1) 页。

**速查表（验收用）**

| 纸张 | 正面（左 \| 右） | 反面（左 \| 右） |
| --- | --- | --- |
| **N' = 8（S=2）** | | |
| 第 1 张 | 8 \| 1 | 2 \| 7 |
| 第 2 张 | 6 \| 3 | 4 \| 5 |
| **N' = 12（S=3）** | | |
| 第 1 张 | 12 \| 1 | 2 \| 11 |
| 第 2 张 | 10 \| 3 | 4 \| 9 |
| 第 3 张 | 8 \| 5 | 6 \| 7 |

**页面适配规则**

- 原页面等比缩放至纸张的一半区域（A5 大小），在该半区内**居中**；
- 装订侧（两张页面的相邻边）预留 6 mm 边距，外侧边距 4 mm（MVP 固定值，不提供自定义）；
- 横向页面：自动旋转 90 度以适配竖版小册页面（宽 > 高的页面）；
- 混合尺寸页面：各自独立等比缩放适配，不裁切内容。

### F4 预览

**视图一：纸张视图（默认）**

- 按打印出纸顺序逐张显示：第 1 张正面 → 第 1 张反面 → 第 2 张正面 → …；
- 每张显示为一个横向 A4 页面缩略图，左右两半即两个小册页，页面上标注原 PDF 页码；
- 支持上一张/下一张切换与页码跳转。

**视图二：翻页视图**

- 模拟成品折叠后的翻页效果，按阅读顺序展示（封面 → 第 2 页 → … → 封底）；
- 用户可用左右箭头或点击翻页；
- 每页右下角显示原 PDF 页码角标，空白页显示"空白页"角标；
- MVP 用 CSS 3D transform 实现翻页动效，不要求物理级真实感。

**通用规则**

- 两个视图共享数据，切换不重新解析；
- 预览基于 pdf.js 渲染真实页面内容（不是色块占位）；
- 提供视图切换 tab：纸张视图 / 翻页视图。

### F5 导出

**模式一：双面打印（单 PDF）**

- 生成 1 个 PDF，共 2S 页，页序为：纸 1 正、纸 1 反、纸 2 正、纸 2 反……；
- 每页即一张横向 A4 纸张的一面；
- 导出时提示："打印时请选择双面打印 + 短边翻转，缩放设为 100%"；
- 文件命名：`{原文件名}-booklet-duplex.pdf`。

**模式二：手动双面（两个 PDF）**

- 生成 2 个 PDF：
  - 正面文件：共 S 页，依次为纸 1 正、纸 2 正……，命名 `{原文件名}-booklet-front.pdf`；
  - 反面文件：共 S 页，依次为纸 1 反、纸 2 反……，命名 `{原文件名}-booklet-back.pdf`；
- 每个文件内页面顺序 = 打印队列顺序，用户无需手动设置打印范围。

**通用规则**

- 导出使用 pdf-lib 在本地生成并触发浏览器下载；
- 三个文件（双面 1 个 / 手动 2 个）均从同一拼版数据生成，保证一致；
- 导出过程显示进度提示，完成前禁止重复点击。

### F6 手动双面打印指引

**交互逻辑**

- 选择手动双面模式后，导出完成页显示图文步骤；
- 步骤内容：
  1. 先打印"正面文件"，打印设置：实际大小（100%）、横向、单面；
  2. 取出打印好的纸，**保持原方向**直接放回进纸器（空白面朝上）；
  3. 打印"反面文件"，同样设置；
  4. 按纸张顺序叠放，沿中线对折，装订。
- 提供提示条："不同打印机进纸方向可能不同，建议先用 2 张纸试印一次；如果反面方向反了，把纸旋转 180 度再放回。"
- MVP 提供固定指引文案 + 简单示意图，不做按打印机型号的个性化指引。

## 6. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 隐私 | 文件全程本地处理，不发起任何包含文件内容的网络请求；页面可被 DevTools Network 面板验证 |
| 性能 | 50 页以内 PDF：上传到可预览 ≤ 3 秒；导出 ≤ 5 秒（普通办公电脑） |
| 兼容性 | Chrome / Edge 最新两个大版本；桌面端优先，移动端可预览但以桌面使用为主要目标 |
| 部署 | 纯静态站点，无后端，可部署到 Vercel / Netlify / GitHub Pages |

## 7. 技术方案建议

- 框架：Vite + React（MVP 无 SSR 需求，Vite 更轻）；
- PDF 解析与生成：`pdf-lib`（客户端，支持页面复制、旋转、缩放与新文档组装）；
- 预览渲染：`pdf.js`（渲染真实页面内容到 canvas）；
- 翻页动效：CSS 3D transform；
- 部署：Vercel 或 Netlify 静态托管。

## 8. 验收标准

1. 上传一份 12 页 PDF，预览拼版与 F3 速查表 N'=12 一致；
2. 上传一份 10 页 PDF，自动提示补 2 页空白，空白页在两种视图中均有角标，拼版正确（末 2 页为空白）；
3. 双面单 PDF 共 2S 页，用 pdf.js 抽查第 1、2、最后 1 页，页序正确；
4. 手动双面两个 PDF 各 S 页，对应每张纸的正/反面；
5. 打开 DevTools Network 面板，上传、预览、导出全过程中无文件内容网络请求；
6. 上传加密 PDF、损坏 PDF、非 PDF 文件，分别出现对应错误提示，不进入预览；
7. 12 页 PDF 从上传到可预览耗时 ≤ 3 秒。

## 9. 二期展望（Backlog）

1. 半页拼版：一张 A4 纸打印两本相同小册子，裁开后得两本；需要裁切参考线；
2. 爬移补偿：页数较多时内侧页面向外微移；
3. Letter 纸张与自定义尺寸；
4. 装订参考线（折线、钉位置）可选叠加；
5. 批量处理；
6. PWA 离线使用；
7. 打印机适配指引社区知识库（按品牌/型号收录放纸方向）。
